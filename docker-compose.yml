services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: mapagent
      POSTGRES_PASSWORD: mapagent
      POSTGRES_DB: mapagent
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
